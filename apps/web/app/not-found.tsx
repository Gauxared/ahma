/**
 * Экран «адреса нет» — на русском и с ответом на вопрос «что теперь».
 *
 * ЧТО ЭТО ЗАКРЫВАЕТ
 *
 * Пройдено руками по пути Г-15: второй арендатор открывает `/objects/WALK-1` —
 * чужой объект, — и получает стандартную страницу Next: чёрный текст «404 This
 * page could not be found» на белом фоне, без оболочки, без языка продукта.
 *
 * Это не косметика. Экран показывается ровно тогда, когда человек попал не
 * туда, — то есть в момент, когда ему нужнее всего понять, что произошло и куда
 * идти. Английская заглушка отвечает «сломалось», хотя система как раз
 * сработала правильно: адрес закрыт границей арендатора либо объекта нет.
 *
 * ПОЧЕМУ НЕ НАЗЫВАЕМ ПРИЧИНУ ТОЧНЕЕ
 *
 * «Объекта нет» и «объект чужой» здесь СОЗНАТЕЛЬНО не различаются. Разные
 * ответы на эти два случая — это способ узнать, какие шифры заведены у соседа:
 * перебором адресов. Та же причина, по которой вход не говорит, существует ли
 * адрес почты.
 *
 * Оболочки здесь нет намеренно: `not-found` показывается и до того, как стало
 * известно, кто смотрит, и рельс с чужой личностью был бы хуже его отсутствия.
 */
import { Card, CardBody } from "@web/src/ui/kit/card.js";
import { Page, PageHead } from "@web/src/ui/kit/page.js";
import { StateEmpty } from "@web/src/ui/kit/states.js";

export default function NotFound() {
  return (
    <Page>
      <PageHead note="Такого адреса в системе нет." title="Страница не найдена" />
      <Card>
        <CardBody>
          <StateEmpty title="Адрес не открывается">
            Либо страницы с таким адресом не существует, либо она принадлежит другой организации. Проверьте шифр
            объекта — он виден в реестре.{" "}
            <a href="/">Пульт</a> · <a href="/objects">Реестр объектов</a>
          </StateEmpty>
        </CardBody>
      </Card>
    </Page>
  );
}
