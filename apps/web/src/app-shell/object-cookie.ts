/**
 * Имя cookie выбранного объекта — отдельным модулем, и по той же причине,
 * что имя cookie рельса (`Ф-58`): из модуля с `"use server"` наружу видно
 * только асинхронные функции, и константа туда не укладывается.
 */
export const OBJECT_COOKIE = "si_object";
