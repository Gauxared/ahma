/**
 * Имя cookie свёрнутости рельса — отдельным модулем, и не ради порядка.
 *
 * Константа лежала рядом с серверным действием, и сборка это отвергла:
 * «Only async functions are allowed to be exported in a "use server" file».
 * Правило разумное — из такого модуля наружу видны только вызовы, которые
 * пойдут по сети, — поэтому имя переехало сюда, где его читают и запись
 * (`rail-preference.ts`), и чтение (`shell.tsx`).
 */
export const RAIL_COOKIE = "si_rail";

/** Значение cookie, при котором рельс показан узкой полосой. */
export const RAIL_NARROW = "узкий";

/** Значение cookie, при котором рельс показан целиком. */
export const RAIL_WIDE = "широкий";
