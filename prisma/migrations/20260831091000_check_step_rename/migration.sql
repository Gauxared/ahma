-- Приведение имени таблицы к общему виду: остальные таблицы схемы названы в
-- snake_case через @@map, и одна PascalCase-таблица среди них — это ловушка
-- для того, кто будет писать SQL руками.
--
-- Переименование, а не пересоздание: данные прогонов сохраняются.
-- Политика RLS и индексы переезжают вместе с таблицей автоматически.

ALTER TABLE "CheckStep" RENAME TO check_step;
