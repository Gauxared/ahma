-- Агентный режим прогона (Т9.1).
--
-- Режим ХРАНИТСЯ, а не выводится: спецификация требует, чтобы он был виден в
-- артефакте и на экране. Прогон, о котором нельзя сказать, чем он сделан —
-- циклом с инструментами или одним вызовом на роль, — несравним ни с чем.
ALTER TYPE "CheckMode" ADD VALUE IF NOT EXISTS 'agentic';
