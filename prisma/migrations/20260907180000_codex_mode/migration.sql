-- Режим прогона «агенты» на Codex SDK (spec-demo-agent А1).
--
-- Четвёртое значение рядом с тремя прежними, а не замена `agentic`: прежний
-- агентный путь остаётся выбором, и прогоны обоих видов должны различаться в
-- артефакте и на экране — иначе сравнение путей невозможно.
ALTER TYPE "CheckMode" ADD VALUE IF NOT EXISTS 'codex';
